
1. If i have browser with many items to select and I create an Array to select few items and the array is not in order of items available in list.......how to handle that situation.

2. While working on contains --- if(itemsToOrderList.contains(availableItems)) ---- which one should be under contains and which one should be before?

3. 
